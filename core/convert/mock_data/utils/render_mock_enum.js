/**
 * @description
 * 渲染 mock 枚举数据
 * @param {string|array} enumList
 * @param {string} type
 */
function renderMockEnum({ content, rule }, type) {
  // TODO: 待优化
  return { content, rule };
}
module.exports = renderMockEnum;
