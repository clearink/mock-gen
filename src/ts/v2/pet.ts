export namespace Add a new pet to the store {
  // body 参数
  export interface BodyParams {
    [object Object]
  }
  // query 参数
  export interface QueryParams {
    [object Object]
  }
  // 响应 数据
  export interface ResponseData {
    [object Object]
  }
}export namespace Update an existing pet {
  // body 参数
  export interface BodyParams {
    [object Object]
  }
  // query 参数
  export interface QueryParams {
    [object Object]
  }
  // 响应 数据
  export interface ResponseData {
    [object Object]
  }
}